pub struct GameSystem {
    pub id: i32,
    pub system_name: String,
}

pub struct Contact {
    pub id: i32,
    pub email: String,
}

pub struct Group {
    pub id: i32,
    pub name: String,
}

pub struct Website {
    pub id: i32,
    pub url: String,
}

pub struct Materials {
    pub id: i32,
    pub summary: String,
}
