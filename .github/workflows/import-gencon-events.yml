name: Import GenCon Events

on:
  push:
    branches: [ parse-json ]

jobs:
  import-json:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        repository: ${{ github.event.pull_request.head.repo.full_name }}
        ref: ${{ github.event.pull_request.head.ref }}
    - name: Download Zip File
      run: curl https://www.gencon.com/downloads/events.zip -L -o events.zip
    - name: Unzip file
      run: 7z x events.zip
    - name: Install convert-excel-to-json
      run: |
        npm install convert-excel-to-json \
        npx convert-excel-to-json --sourceFile="events.xlsx" > ./frontend/assets/events/test.json
    - uses: EndBug/add-and-commit@v9
      with:
        remove: './frontend/assets/events/test3.json'
        add: './frontend/assets/events/test.json'