
pub struct GameSystem {
    id: i32,
    system_name: String,
}

pub struct Contact {
    id: i32,
    email: String,
}

pub struct Group {
    id: i32,
    name: String,
}

pub struct Website {
    id: i32,
    url: String,
}

pub struct Materials {
    id: i32,
    summary: String,
}