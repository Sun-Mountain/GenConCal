export default function handleChoice (
  action,
  choice,
  setChoice,
  gameId,
  list,
  setList
) {
  switch(action) {
    case 'addChoice':

    case 'removeChoice':
    default:
      return console.log(ERROR);
  }
}